// LinkedList for our locations

#include "LinkedList.h"
using namespace std;

// add default constructors/getter setters.

// We will be initializing this linkedList with NO parameters
// it'll start empty

// Add a push_back to allow up to add as many 
// locations as we want when generating